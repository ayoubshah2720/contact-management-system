export const environment = {
  // Set to `true` to use the bundled JSON seed files under `public/mock-data`.
  useLocalMockData: false,
  // Replace with your MockAPI project base URL, for example:
  // https://your-project-id.mockapi.io/api/v1
  apiBaseUrl: 'https://your-project-id.mockapi.io/api/v1'
};
